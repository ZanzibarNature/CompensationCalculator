version: '3'
services:
  compensationcalculator:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
    environment:
      - DEV_MODE=true
      - RAPID_API_KEY=YOUR_API_KEY